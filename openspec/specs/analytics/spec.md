### Requirement: Расходы по периодам (v2)

Блок «Календарь расходов» на странице `/transactions` SHALL отображать столбчатую диаграмму с группировкой по дням (внутри месяца) и по месяцам (внутри года), с переключением между этими двумя уровнями детализации. Группировка по неделям и произвольный диапазон дат остаются нереализованными в этой версии требования. Отдельной страницы `/analytics` нет — календарь встроен блоком над списком транзакций.

#### Scenario: Группировка по дням месяца
- **WHEN** пользователь открывает страницу `/transactions` с календарём в месячном виде
- **THEN** график показывает сумму расходов по каждому дню выбранного месяца

#### Scenario: Группировка по месяцам года
- **WHEN** пользователь переключается на годовой вид
- **THEN** график показывает сумму расходов по каждому месяцу выбранного года

#### Scenario: Общая сумма за период
- **WHEN** пользователь просматривает месячный или годовой вид
- **THEN** под графиком отображается название периода (например, «Июнь 2026» или «2026») и суммарные расходы за этот период

### Requirement: Структура расходов по тегам (v2)

Система SHALL отображать круговую или treemap-диаграмму структуры расходов по тегам за выбранный период.

#### Scenario: Доли тегов в расходах
- **WHEN** пользователь открывает график структуры по тегам за месяц
- **THEN** отображаются доли каждого тега (и подтегов при детализации) от общей суммы расходов

### Requirement: Доходы vs расходы (v2)

Система SHALL показывать сравнительный график доходов и расходов по месяцам.

#### Scenario: Сравнение за квартал
- **WHEN** пользователь выбирает диапазон «квартал»
- **THEN** график показывает доходы и расходы по месяцам внутри квартала для визуального сравнения

## 1. Создание пользователей

<!-- spec: user-accounts -->

### TC-01: Успешное создание пользователя через форму

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Создание учётной записи → Успешное создание пользователя |
| Шаги | 1. Открыть `/admin/users` 2. Заполнить `display_name="Anna"`, `username="anna_k"` 3. Нажать «Создать» |
| Ожидаемый результат | Пользователь появляется в таблице; `GET /api/v1/users` возвращает запись с `username="anna_k"` |
| Статус | ☐ |

### TC-02: Дубликат username (без учёта регистра)

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Создание учётной записи → Дубликат username |
| Шаги | 1. Создать пользователя `username="Simon"` 2. Попытаться создать `username="simon"` |
| Ожидаемый результат | Ошибка `USERNAME_TAKEN`; второй пользователь не создан |
| Статус | ☐ |

### TC-03: Невалидный username — спецсимволы

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Negative |
| Спека | Создание учётной записи → Невалидный username |
| Шаги | 1. Попытаться создать пользователя с `username="my user!"` |
| Ожидаемый результат | Ошибка `INVALID_USERNAME`; форма подсвечивает поле |
| Статус | ☐ |

### TC-04: Невалидный username — слишком короткий

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Boundary |
| Спека | Создание учётной записи → Невалидный username |
| Шаги | 1. Попытаться создать пользователя с `username="ab"` (2 символа) |
| Ожидаемый результат | Ошибка `INVALID_USERNAME` |
| Статус | ☐ |

## 2. Вход без пароля

<!-- spec: user-accounts -->

### TC-05: Вход через список пользователей

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Вход без пароля → Вход существующего пользователя через список |
| Шаги | 1. Открыть приложение без `localStorage.activeUserId` 2. Увидеть экран входа 3. Кликнуть на карточку `@simon` |
| Ожидаемый результат | Переход на `/projects`; `localStorage.activeUserId` установлен |
| Статус | ☐ |

### TC-06: Вход по @username — пользователь не найден

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Вход без пароля → Вход по @username |
| Шаги | 1. Ввести `@ghost` в поле на экране входа 2. Нажать «Войти» |
| Ожидаемый результат | Отображается ошибка `USER_NOT_FOUND`; переход не происходит |
| Статус | ☐ |

### TC-07: Повторный визит с сохранённым пользователем

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Вход без пароля → Повторный визит |
| Шаги | 1. Установить `localStorage.activeUserId` вручную 2. Открыть приложение |
| Ожидаемый результат | Экран входа пропущен; переход сразу на `/projects` или `/` (если проект выбран) |
| Статус | ☐ |

## 3. Проекты — создание и переключение

<!-- spec: projects -->

### TC-08: Создание первого проекта

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Создание проекта → Первый вход без проекта |
| Шаги | 1. Войти под новым пользователем без проектов 2. Нажать «Создать проект» 3. Ввести `name="Личные финансы"` 4. Нажать «Создать» |
| Ожидаемый результат | Проект создан; пользователь перенаправлен на главную страницу с активным проектом |
| Статус | ☐ |

### TC-09: Переключение активного проекта

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Переключение активного проекта → Переключение проекта |
| Шаги | 1. Состоять в 2+ проектах 2. Выбрать другой проект в переключателе хедера |
| Ожидаемый результат | `localStorage.activeProjectId` обновлён; данные на странице перезагружены для нового проекта |
| Статус | ☐ |

### TC-10: Один проект — переключатель скрыт

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Переключение активного проекта → Один проект |
| Шаги | 1. Войти под пользователем с одним проектом 2. Открыть хедер |
| Ожидаемый результат | Переключатель проектов не отображается |
| Статус | ☐ |

## 4. Участники проекта

<!-- spec: projects -->

### TC-11: Успешное приглашение участника

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Приглашение участника → Успешное приглашение |
| Шаги | 1. Войти как `owner` проекта 2. Открыть настройки проекта 3. Ввести `@anna_k` 4. Нажать «Добавить» |
| Ожидаемый результат | `anna_k` появляется в списке участников с ролью `member` |
| Статус | ☐ |

### TC-12: Приглашение несуществующего пользователя

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Приглашение участника → Пользователь не найден |
| Шаги | 1. Owner вводит `@nobody` в форму добавления участника |
| Ожидаемый результат | Ошибка `USER_NOT_FOUND`; список участников не изменился |
| Статус | ☐ |

### TC-13: Повторное приглашение уже существующего участника

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Negative |
| Спека | Приглашение участника → Участник уже в проекте |
| Шаги | 1. Owner приглашает `@anna_k`, которая уже в проекте |
| Ожидаемый результат | Ошибка `ALREADY_MEMBER` |
| Статус | ☐ |

### TC-14: Member не может удалить участника

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Роли и права участников → Member пытается удалить участника |
| Шаги | 1. Войти как `member` 2. Отправить `DELETE /api/v1/projects/{id}/members/{userId}` |
| Ожидаемый результат | `403 FORBIDDEN` |
| Статус | ☐ |

### TC-15: Единственный owner не может покинуть проект

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Роли и права участников → Owner пытается покинуть проект в одиночку |
| Шаги | 1. Owner (единственный участник) пытается удалить себя из проекта |
| Ожидаемый результат | Ошибка `CANNOT_LEAVE_SOLE_OWNER` |
| Статус | ☐ |

## 5. Изоляция данных

<!-- spec: projects -->

### TC-16: Данные видны только участникам проекта

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Изоляция данных по проекту → Запрос к данным чужого проекта |
| Шаги | 1. Войти как `@anna_k` 2. Отправить запрос с `X-Project-ID` проекта, в котором `@anna_k` не состоит |
| Ожидаемый результат | `403 FORBIDDEN` |
| Статус | ☐ |

### TC-17: Запрос без X-Project-ID

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Изоляция данных по проекту → Запрос без X-Project-ID |
| Шаги | 1. Отправить `GET /api/v1/transactions` без заголовка `X-Project-ID` |
| Ожидаемый результат | `400 BAD_REQUEST` с кодом `PROJECT_ID_REQUIRED` |
| Статус | ☐ |

## 6. Миграция данных

<!-- spec: projects -->

### TC-18: Существующие данные привязываются к дефолтному проекту

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Миграция существующих данных → Применение миграции на непустой БД |
| Шаги | 1. Применить миграцию `000010` на БД с существующими транзакциями и тегами 2. Проверить `SELECT project_id FROM transactions LIMIT 5` |
| Ожидаемый результат | Все строки имеют `project_id` дефолтного проекта; записи не потеряны |
| Статус | ☐ |

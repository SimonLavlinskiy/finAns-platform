## 1. Загрузка CSV-файла

<!-- spec: import-transactions -->

### TC-01: Успешная загрузка корректного CSV

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Загрузка CSV-файла → Успешная загрузка файла |
| Шаги | 1. Открыть `/import`. 2. Перетащить (или выбрать через кнопку) `.csv` файл с 24 строками. |
| Ожидаемый результат | Файл отправлен на `POST /import/batches`; под зоной загрузки отображается «transactions_june.csv · 24 строки»; ниже появляется заполненная таблица модерации |
| Статус | ☐ |

### TC-02: Отклонение файла не-CSV формата

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Negative |
| Спека | Загрузка CSV-файла → Отклонение файла неподходящего формата |
| Шаги | 1. Открыть `/import`. 2. Попытаться загрузить файл `report.xlsx`. |
| Ожидаемый результат | Файл не отправляется на сервер, отображается сообщение о недопустимом формате |
| Статус | ☐ |

### TC-03: Восстановление незакрытого батча после reload

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Загрузка CSV-файла → Восстановление незавершённого импорта после перезагрузки страницы |
| Шаги | 1. Загрузить CSV на `/import`, не завершая импорт. 2. Перезагрузить страницу браузера. |
| Ожидаемый результат | `GET /import/batches/active` возвращает прежний батч; таблица модерации заполнена тем же набором строк, что и до reload |
| Статус | ☐ |

### TC-03b: Открытие справки по формату CSV

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Справка по формату CSV → Открытие справки по формату |
| Шаги | 1. Открыть `/import`. 2. Нажать иконку-вопрос рядом с кнопкой «Выбрать файл». |
| Ожидаемый результат | Открывается окно со списком обязательных (`title`, `amount`, `date`) и необязательных (`tag`, `category`, `specificity`, `comment`, `url`) колонок, требованиями к формату каждого поля и примером строки CSV |
| Статус | ☐ |

### TC-03c: Закрытие справки по формату CSV

| Поле | Значение |
|------|----------|
| Приоритет | Low |
| Тип | Positive |
| Спека | Справка по формату CSV → Закрытие справки |
| Шаги | 1. Открыть справку по формату. 2. Закрыть её (крестик, клик вне окна или Esc). |
| Ожидаемый результат | Окно справки закрывается, зона загрузки остаётся доступной и не сбрасывается |
| Статус | ☐ |

## 2. Валидация строк при парсинге

<!-- spec: import-transactions -->

### TC-04: Нечисловое значение суммы

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Валидация строк при парсинге → Ошибка нечислового значения суммы |
| Шаги | 1. Загрузить CSV со строкой, где `amount = "тысяча"`. |
| Ожидаемый результат | Строка получает статус `Ошибка`; ячейка «Сумма» с красной рамкой и tooltip с описанием ошибки |
| Статус | ☐ |

### TC-05: Неверный формат даты

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Валидация строк при парсинге → Ошибка неверного формата даты |
| Шаги | 1. Загрузить CSV со строкой, где `date = "15.06.2026"`. |
| Ожидаемый результат | Строка получает статус `Ошибка`; ячейка «Дата» с красной рамкой и tooltip |
| Статус | ☐ |

### TC-06: Тег не найден в системе

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Валидация строк при парсинге → Ошибка ненайденного тега |
| Шаги | 1. Загрузить CSV со строкой, где `tag = "Неизвестная категория"` (тега с таким именем/путём нет в системе). |
| Ожидаемый результат | Строка получает статус `Ошибка`; ячейка «Тег» с красной рамкой и tooltip |
| Статус | ☐ |

### TC-07: Тег с иерархическим путём резолвится корректно

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Валидация строк при парсинге (резолюция тега по `/`) |
| Шаги | 1. В системе существует тег «Еда» с подтегом «Кафе». 2. Загрузить CSV со строкой, где `tag = "Еда/Кафе"`. |
| Ожидаемый результат | Строка не помечается ошибкой по полю «Тег»; `tag_id` строки соответствует подтегу «Кафе» |
| Статус | ☐ |

### TC-08: Недопустимое значение category

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Boundary |
| Спека | Валидация строк при парсинге (category не из списка) |
| Шаги | 1. Загрузить CSV со строкой, где `category = "transfer"`. |
| Ожидаемый результат | Строка получает статус `Ошибка`; ячейка «Категория» с красной рамкой и tooltip |
| Статус | ☐ |

### TC-09: Отсутствует обязательное поле

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Boundary |
| Спека | Валидация строк при парсинге (отсутствует `title`/`amount`/`date`) |
| Шаги | 1. Загрузить CSV со строкой без значения в колонке `date`. |
| Ожидаемый результат | Строка получает статус `Ошибка`; ячейка «Дата» подсвечена как обязательная и не заполненная |
| Статус | ☐ |

### TC-10: Сводка ошибок над таблицей

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Валидация строк при парсинге → Сводка ошибок над таблицей |
| Шаги | 1. Загрузить CSV, где строка 3 содержит нечисловую сумму, строка 7 — неверный формат даты, строка 12 — ненайденный тег. |
| Ожидаемый результат | Над таблицей отображается список: «Строка 3: поле "amount" …», «Строка 7: поле "date" …», «Строка 12: поле "tag" …» |
| Статус | ☐ |

## 3. Таблица модерации и inline-редактирование

<!-- spec: import-transactions -->

### TC-11: Inline-редактирование ячейки

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Таблица модерации с inline-редактированием → Inline-редактирование ячейки |
| Шаги | 1. Кликнуть на ячейку «Сумма» строки. |
| Ожидаемый результат | Ячейка переключается в input с текущим значением суммы |
| Статус | ☐ |

### TC-12: Правка ячейки сохраняется на сервере и пересчитывает статус

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Таблица модерации с inline-редактированием → Сохранение правки отправляется на сервер |
| Шаги | 1. У строки со статусом `Ошибка` (амount = «тысяча») исправить «Сумма» на `1250`. |
| Ожидаемый результат | `PATCH /import/rows/{id}` отправлен; сервер возвращает строку без ошибки по «Сумма»; статус строки пересчитан (`Готово`/`Ожидает` в зависимости от прочих полей) |
| Статус | ☐ |

### TC-13: Запрет очистки обязательного поля

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Negative |
| Спека | Таблица модерации с inline-редактированием → Запрет очистки обязательного поля |
| Шаги | 1. Кликнуть на ячейку «Название». 2. Удалить весь текст и попытаться сохранить пустое значение. |
| Ожидаемый результат | Пустое значение не принимается, в ячейке остаётся предыдущее «Название» |
| Статус | ☐ |

### TC-14: Жёлтая рамка незаполненного поля принятия

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Таблица модерации с inline-редактированием → Жёлтая рамка незаполненного поля принятия |
| Шаги | 1. Загрузить строку без ошибок парсинга, но с пустой колонкой `category`. |
| Ожидаемый результат | Ячейка «Категория» подсвечена жёлтой рамкой, статус строки — `Ожидает` |
| Статус | ☐ |

### TC-15: Inline-создание нового тега через TagFormPicker

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Таблица модерации с inline-редактированием (тот же компонент дерева тегов, что у формы транзакции) |
| Шаги | 1. В ячейке «Тег» строки открыть пикер. 2. Создать новый тег прямо в пикере. |
| Ожидаемый результат | Новый тег создаётся (как и в форме транзакции), сразу применяется к строке, ячейка «Тег» обновляется без перезагрузки страницы |
| Статус | ☐ |

## 4. Статусы строки

<!-- spec: import-transactions -->

### TC-16: Ошибка → Готово после исправления

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Статусы строки и автоматический пересчёт → Исправление ошибки переводит строку в Готово |
| Шаги | 1. У строки со статусом `Ошибка` исправить проблемное поле. 2. Заполнить `tag`/`category`/`specificity`, если были пустыми. |
| Ожидаемый результат | Статус строки автоматически становится `Готово` |
| Статус | ☐ |

### TC-17: Ошибка → Ожидает после исправления

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Статусы строки и автоматический пересчёт → Исправление ошибки переводит строку в Ожидает |
| Шаги | 1. У строки со статусом `Ошибка` исправить проблемное поле (например, дату). 2. Оставить `category` незаполненной. |
| Ожидаемый результат | Статус строки становится `Ожидает` (не `Готово`) |
| Статус | ☐ |

## 5. Панель управления

<!-- spec: import-transactions -->

### TC-18: «Выбрать все готовые» выделяет только Готово

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Панель управления над таблицей → Выбор всех готовых строк |
| Шаги | 1. В батче есть строки во всех статусах. 2. Отметить чекбокс «Выбрать все готовые». |
| Ожидаемый результат | Выделены только строки со статусом `Готово`; `Ожидает` и `Ошибка` не выделены |
| Статус | ☐ |

### TC-19: Счётчики обновляются в реальном времени

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Панель управления над таблицей → Счётчик обновляется при изменении строки |
| Шаги | 1. Исправить строку из `Ошибка` в `Готово`. |
| Ожидаемый результат | Счётчики «Ошибок» уменьшается на 1, «Готово» увеличивается на 1 без перезагрузки страницы |
| Статус | ☐ |

## 6. Принятие строк в транзакции

<!-- spec: import-transactions -->

### TC-20: Disabled-кнопка «Принять» с перечислением проблем

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Negative |
| Спека | Принятие строк в транзакции → Disabled-кнопка с перечислением проблем |
| Шаги | 1. У строки со статусом `Ожидает` не заполнены «Тег» и «Категория». 2. Навести курсор на кнопку «Принять». |
| Ожидаемый результат | Кнопка disabled; tooltip показывает «Заполните: Тег, Категория» |
| Статус | ☐ |

### TC-21: Одиночное принятие готовой строки

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Принятие строк в транзакции → Одиночное принятие готовой строки |
| Шаги | 1. У строки со статусом `Готово` нажать «Принять». |
| Ожидаемый результат | `POST /import/rows/{id}/accept` создаёт транзакцию и удаляет строку из `moderation_transactions`; баланс в хедере пересчитан; строка на 1–2 сек показана как «Перенесено», затем исчезает из списка |
| Статус | ☐ |

### TC-22: Массовое принятие с подтверждением

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Принятие строк в транзакции → Массовое принятие с подтверждением |
| Шаги | 1. Выбрать 12 готовых строк. 2. Нажать «Принять выбранные (12)». 3. Подтвердить в диалоге «Добавить 12 транзакций в таблицу расходов?». |
| Ожидаемый результат | `POST /import/batches/{id}/accept` создаёт 12 транзакций и удаляет 12 строк; счётчик «Перенесено» увеличивается на 12 |
| Статус | ☐ |

### TC-23: Отмена диалога массового принятия

| Поле | Значение |
|------|----------|
| Приоритет | Low |
| Тип | Negative |
| Спека | Принятие строк в транзакции (confirmation) — boundary |
| Шаги | 1. Выбрать готовые строки. 2. Нажать «Принять выбранные». 3. Нажать «Отмена» в диалоге. |
| Ожидаемый результат | Запрос на принятие не отправляется, строки остаются в статусе `Готово` и выделены |
| Статус | ☐ |

### TC-24: Ошибка backend при принятии не удаляет строку

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Negative |
| Спека | Принятие строк в транзакции — boundary (отказ запроса) |
| Шаги | 1. Нажать «Принять» на готовой строке в момент, когда backend возвращает 5xx. |
| Ожидаемый результат | Строка остаётся в таблице со статусом `Готово`, отображается тост об ошибке, повторное «Принять» доступно |
| Статус | ☐ |

## 7. Завершение импорта

<!-- spec: import-transactions -->

### TC-25: Баннер завершения после переноса всех готовых строк

| Поле | Значение |
|------|----------|
| Приоритет | High |
| Тип | Positive |
| Спека | Завершение импорта → Баннер завершения после переноса всех готовых строк |
| Шаги | 1. В батче из 24 строк принять все 22 строки без ошибок (2 остаются `Ошибка`). |
| Ожидаемый результат | Появляется баннер «Импорт завершён: 22 транзакции добавлены, 2 строки с ошибками» с кнопками «Перейти к транзакциям» / «Загрузить новый файл» |
| Статус | ☐ |

### TC-26: Переход в транзакции из баннера

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Завершение импорта (кнопка «Перейти к транзакциям») |
| Шаги | 1. На экране баннера завершения нажать «Перейти к транзакциям». |
| Ожидаемый результат | Происходит редирект на `/transactions` |
| Статус | ☐ |

### TC-27: Сброс страницы для нового импорта

| Поле | Значение |
|------|----------|
| Приоритет | Medium |
| Тип | Positive |
| Спека | Завершение импорта → Сброс страницы для нового импорта |
| Шаги | 1. На экране баннера завершения (или в процессе импорта) нажать «Загрузить новый файл». |
| Ожидаемый результат | `POST /import/batches/{id}/close` закрывает батч; таблица, счётчики и баннер сбрасываются; отображается пустая зона загрузки |
| Статус | ☐ |
